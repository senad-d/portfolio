---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<div class="mb-4">
  <h2 class="text-2xl font-semibold">{title}</h2>
  {description && <p class="mt-2 text-slate-300">{description}</p>}
</div>
